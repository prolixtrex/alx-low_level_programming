#include <stdio.h>
#include <ctype.h>

/**
 * main - a programe that prints the values of hex in lower case
 * All codes must be inside main function
 * Return: 0
 */

int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		putchar(x % 10 + '0');
	}

	for (x = 'a'; x <= f; x++)
	{
		putchar(x);
	}

	putchar('\n');
	return (0);
}
