#include <stdlib.h>
#include "main.h"

/**
 * _abs - a function that returns the absolute value of the input
 * @int: the value to check
 * Return: return value
 */

int _abs(int c)
{
	return (abs(c));
}
